services:
  web-dev:
    build: .
    container_name: landing_page_dev
    ports:
      - "8080:8080"
    volumes:
      - ./workspace:/workspace
    working_dir: /workspace
    command: live-server --port=8080 --host=0.0.0.0
    stdin_open: true
    tty: true

